{
  "mcpServers": {
    "v0": {
      "command": "npx",
      "args": [
        "-y",
        "@vercel/v0-mcp"
      ],
      "env": {
        "V0_API_KEY": "${V0_API_KEY}"
      }
    }
  }
}

